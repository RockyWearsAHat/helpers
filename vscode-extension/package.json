{
  "name": "git-shell-helpers",
  "displayName": "GitHub Shell Helpers",
  "description": "Settings for git-shell-helpers community cache participation and DevOps audit configuration.",
  "version": "0.3.7",
  "publisher": "RockyWearsAHat",
  "license": "MIT",
  "engines": {
    "vscode": "^1.110.0"
  },
  "enabledApiProposals": [
    "chatParticipantPrivate"
  ],
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onLanguageModelTool:gsh-inspect-copilot-customization-warnings",
    "onLanguageModelTool:gsh-strict-lint"
  ],
  "main": "./extension.js",
  "contributes": {
    "mcpServerDefinitionProviders": [
      {
        "id": "gitShellHelpers.mcpServers",
        "label": "GitHub Shell Helpers"
      }
    ],
    "commands": [
      {
        "command": "gitShellHelpers.showCommunityStatus",
        "title": "GitHub Shell Helpers: Show Community Cache Status"
      },
      {
        "command": "gitShellHelpers.inspectCopilotCustomizationWarnings",
        "title": "GitHub Shell Helpers: Run Strict Linting"
      },
      {
        "command": "gitShellHelpers.searchArchivedChatHistory",
        "title": "GitHub Shell Helpers: Search Archived Chat History"
      },
      {
        "command": "gitShellHelpers.loginGitHub",
        "title": "GitHub Shell Helpers: Sign in to GitHub"
      },
      {
        "command": "gitShellHelpers.logoutGitHub",
        "title": "GitHub Shell Helpers: Sign out of GitHub"
      },
      {
        "command": "gitShellHelpers.selectRepos",
        "title": "GitHub Shell Helpers: Select Whitelisted Repositories"
      },
      {
        "command": "gitShellHelpers.restartMcpServer",
        "title": "GitHub Shell Helpers: Restart MCP Server"
      },
      {
        "command": "gitShellHelpers.openMcpServerControls",
        "title": "GitHub Shell Helpers: Open MCP Server Controls"
      },
      {
        "command": "gitShellHelpers.refreshModels",
        "title": "GitHub Shell Helpers: Refresh Available Models"
      },
      {
        "command": "gitShellHelpers.openModelPicker",
        "title": "GitHub Shell Helpers: Set Active Chat Model"
      },
      {
        "command": "gitShellHelpers.formatOpenFiles",
        "title": "GitHub Shell Helpers: Format All Open Files"
      }
    ],
    "languageModelTools": [
      {
        "name": "gsh-inspect-copilot-customization-warnings",
        "displayName": "Strict Linting (Customization File)",
        "userDescription": "Read live VS Code errors, warnings, hover details, and quick fixes for a Copilot customization file's frontmatter tools list.",
        "modelDescription": "Use this tool when working on Copilot customization files such as .agent.md, .instructions.md, .prompt.md, SKILL.md, or copilot-instructions.md and you need the real VS Code diagnostics for the frontmatter tools list. It reads editor errors, warnings, hover details, and available quick fixes, then returns a readable report directly in chat.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Optional absolute path to a Copilot customization file to inspect. When omitted, the active editor file is used."
            }
          }
        }
      },
      {
        "name": "gsh-strict-lint",
        "displayName": "Strict Linting",
        "userDescription": "Read live VS Code errors and warnings for any file, folder, or the whole workspace.",
        "modelDescription": "Use this tool to get real VS Code diagnostics (errors, warnings, info) for any file, folder, or the entire workspace. Pass filePath for a single file, folderPath to scan a directory recursively, or omit both for a full workspace scan. Use severityFilter to limit results to errors-only or warnings-and-above.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "filePath": {
              "type": "string",
              "description": "Absolute path to a specific file to lint. Omit to scan a folder or the whole workspace."
            },
            "folderPath": {
              "type": "string",
              "description": "Absolute path to a folder. Returns diagnostics for all files within it."
            },
            "severityFilter": {
              "type": "string",
              "enum": [
                "all",
                "warnings-and-above",
                "errors-only"
              ],
              "default": "all",
              "description": "Filter by minimum severity. Defaults to 'all'."
            }
          }
        }
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "gitShellHelpers",
          "title": "GitHub Shell Helpers",
          "icon": "media/git-helpers.svg"
        }
      ]
    },
    "views": {
      "gitShellHelpers": [
        {
          "type": "webview",
          "id": "gitShellHelpers.communityCache",
          "name": "GitHub Shell Helpers",
          "icon": "media/git-helpers.svg",
          "visibility": "visible"
        },
        {
          "id": "gitShellHelpers.worktreeFiles",
          "name": "Branch Files",
          "icon": "media/git-helpers.svg",
          "visibility": "visible",
          "when": "config.gitShellHelpers.branchSessions.enabled"
        }
      ]
    },
    "configuration": {
      "title": "GitHub Shell Helpers",
      "properties": {
        "gitShellHelpers.checkpoint.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable git-checkpoint in this workspace."
        },
        "gitShellHelpers.checkpoint.autoPush": {
          "type": "boolean",
          "default": false,
          "description": "Automatically push to remote after every checkpoint commit."
        },
        "gitShellHelpers.checkpoint.sign": {
          "type": "boolean",
          "default": false,
          "description": "GPG-sign checkpoint commits (requires a GPG key configured in git)."
        },
        "gitShellHelpers.checkpoint.useAI": {
          "type": "boolean",
          "default": true,
          "description": "Generate commit messages using AI. When off, -m is required."
        },
        "gitShellHelpers.checkpoint.model": {
          "type": "string",
          "default": "",
          "description": "Model to use for AI commit message generation. Leave blank to auto-select the cheapest available model."
        },
        "gitShellHelpers.customizationInspector.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Strict Linting, a chat tool that reads live VS Code errors, warnings, and quick fixes for Copilot customization files."
        },
        "gitShellHelpers.branchSessions.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable branch session tools in the MCP server. When on, agents can create chat-bound git worktrees via branch_session_start/end and the workspace follows the active chat's branch. When off (default), branch tools are hidden and Copilot's built-in session management is used instead."
        },
        "gitShellHelpers.branchSessions.orphanSafetyCommit": {
          "type": "boolean",
          "default": true,
          "description": "On startup, scan for orphaned branch session worktrees with uncommitted changes and auto-commit them to prevent work loss. The worktree is then removed but the branch and commits are preserved."
        },
        "gitShellHelpers.branchSessions.autoCleanMerged": {
          "type": "boolean",
          "default": true,
          "description": "On startup, automatically delete local feature/ and fix/ branches that are fully merged into the current baseline branch (dev, main). Prevents accumulation of stale branches from completed sessions."
        },
        "gitShellHelpers.sessionMemory.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Engram-style session memory. Agents log actions and outcomes to a per-workspace session log with TF-IDF indexing and surprise-weighted retrieval. Helps agents learn from past mistakes and adapt across sessions."
        },
        "gitShellHelpers.chatArchive.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable lossless chat history archiving. Captures everything said in Copilot chat sessions — user messages, assistant responses, tool calls — into Brotli-compressed chunks with Bloom filter indexes. Agents can search past conversations via MCP tools."
        },
        "gitShellHelpers.chatArchive.autoCompact": {
          "type": "boolean",
          "default": true,
          "description": "Automatically run L2 compaction when a session accumulates enough chunks. Merges small compressed chunks into larger super-chunks at higher compression, reducing file count and storage. Always lossless and reversible."
        },
        "gitShellHelpers.formatControl.bypassOnAgentSave": {
          "type": "boolean",
          "default": false,
          "description": "Suppress editor.formatOnSave and editor.codeActionsOnSave while enabled. Prevents Prettier, ESLint auto-fix, and other formatters from running on every agent file save. Use the 'Format All Open Files' command to apply formatting once at the end of a request."
        },
        "gitShellHelpers.localSubagents.ollama.host": {
          "type": "string",
          "default": "http://127.0.0.1:11434",
          "description": "Ollama server URL used by the ollama_subagent and ollama_list_models MCP tools."
        },
        "gitShellHelpers.localSubagents.ollama.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Default Ollama model tag (e.g. 'llama3.1', 'qwen2.5-coder:14b') used when ollama_subagent is called without an explicit model. Leave blank to require the caller to specify one."
        },
        "gitShellHelpers.localSubagents.ollama.maxIterations": {
          "type": "integer",
          "default": 12,
          "minimum": 1,
          "maximum": 50,
          "description": "Maximum tool-use rounds the local Ollama sub-agent may run before being forced to summarize and finish."
        },
        "gitShellHelpers.localSubagents.ollama.timeoutSeconds": {
          "type": "integer",
          "default": 300,
          "minimum": 10,
          "maximum": 3600,
          "description": "Total wall-clock timeout for one ollama_subagent invocation."
        },
        "gitShellHelpers.localSubagents.ollama.allowWrite": {
          "type": "boolean",
          "default": false,
          "description": "Allow the local Ollama sub-agent to write files inside the workspace. Off by default — enable only when you trust the chosen model."
        },
        "gitShellHelpers.localSubagents.ollama.allowShell": {
          "type": "boolean",
          "default": false,
          "description": "Allow the local Ollama sub-agent to execute shell commands in the workspace root. Off by default — destructive."
        },
        "gitShellHelpers.localSubagents.openclaw.binary": {
          "type": "string",
          "default": "openclaw",
          "description": "Path or command name for the OpenClaw CLI used by the openclaw_task and openclaw_status MCP tools."
        },
        "gitShellHelpers.localSubagents.openclaw.gatewayUrl": {
          "type": "string",
          "default": "http://127.0.0.1:18789",
          "description": "URL of the local OpenClaw gateway daemon. Used to verify the gateway is up before dispatching tasks."
        },
        "gitShellHelpers.localSubagents.openclaw.defaultThinking": {
          "type": "string",
          "enum": ["off", "low", "medium", "high"],
          "default": "medium",
          "description": "Default --thinking level forwarded to openclaw agent when openclaw_task does not specify one."
        },
        "gitShellHelpers.localSubagents.openclaw.timeoutSeconds": {
          "type": "integer",
          "default": 600,
          "minimum": 10,
          "maximum": 7200,
          "description": "Wall-clock timeout for one openclaw_task invocation."
        }
      }
    }
  }
}
