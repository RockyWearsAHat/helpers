## Highlights

- Fixed global discovery of the `git_checkpoint` MCP tool by having the Helpers VS Code extension publish the bundled `helpers` MCP server across workspaces.
- Updated the macOS package to install the combined MCP server runtime and the Helpers VS Code extension so checkpoint tooling is available after install.
- Bumped the application and extension version to `0.3.4`.
