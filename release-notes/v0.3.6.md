## Highlights

- Added an Engram-inspired session memory system for agents, including MCP tools, indexing, sidebar configuration, and test coverage.
- Fixed workspace-root resolution so MCP tools target the active VS Code workspace instead of falling back to the Helpers install directory.
- Shipped call-time subagent model routing improvements, including language-model discovery, a stronger sentinel-based VS Code patch, and clearer diagnostics when the local model cache cannot be written.
- Hardened `list_language_models` by extracting it into a dedicated module and improving home-directory and error-path handling.

## Quality

- Added targeted tests for `list_language_models`.
- Made the `resolveRepoRoot` test robust across Windows path formats.
- Cleaned up shell patterns that CI shellcheck treats as warnings/failures.
- Repaired the invalid community-cache candidate packet required by the current validation gate.
