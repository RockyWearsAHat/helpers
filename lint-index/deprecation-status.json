{
    "_doc": "COMPLETION PASS 13 \u2014 the ONE datum the attestation faculty cannot derive structurally. A documentation author marks status in a frontmatter block-sequence ENUM (MDN `status:` = deprecated/experimental/non-standard; the attestation faculty DISCOVERS that enum by SHAPE, never by name). But which enum VALUE means PROHIBITION ('no longer use this') is not a structural fact \u2014 every value renders an invariant, subject-varying banner, so structure alone cannot tell the deprecation banner from the experimental one (the measured PASS 11/12 wall). That single choice lives here as DATA, exactly as sources.json carries the doc sources. No banner text, no site, no element, no English \u2014 only the author's own enum value(s) that denote prohibition. Language expansion adds its markers' values here (Rust `deprecated`, Python `deprecated`). PASS 22 \u2014 `removed` is the second such datum: the token that, PAIRED with a prohibition token in one class value (`deprecated-removed`), denotes REMOVAL \u2014 the strongest prohibition status, a WHOLE-MODULE marker distinct from an inline per-method `versionmodified deprecated` note (the PASS-21 precision line, measured: the tight compound matches 29 pages and admits exactly the removed python modules, while a loose `deprecated` substring floods 2812 and contaminates the subject basis). Consumed ONLY by the construction miner's removal-subject basis (`lint_attest::attests_module_removal`); the page-role attestation (`attests`, `prohibition_class_tokens`) is UNTOUCHED, so every existing module is byte-identical.",
    "_doc_pass28": "COMPLETION PASS 28/29 \u2014 the NOTE-SCOPE register ANCHORS, the third and fourth such data. A rendered deprecation note's OWN sentence can narrow its scope: `scope_exception` anchors the exception register (an item named AFTER an exception-meaning word is EXCLUDED from the deprecation \u2014 'All TLSVersion members except TLSVersion.TLSv1_3 are deprecated'); `usage_form` anchors the conditional register in the FIRST SENTENCE of the clause after the deprecation head ('Deprecation warning is emitted if loop \u2026'). PASS 29 (owner ruling: registers are MEANING, tokens are shims): each anchor is ONE dictionary word; the meaning net expands it to every word the dictionary DEFINES VIA it under a function-word POS gate ('unless'/'excluding'/'barring' join by their own definitions), and the gerund usage-subject register ('Passing/Setting/Accepting \u2026 is deprecated') is morphological + the dictionary's own verb POS \u2014 no word list exists for it. Consumed only by `lint_lang_layer::note_counter_attests`; measured: identical corpus classification to the PASS-28 token lists, plus dictionary-wide generalization (LINTER.md PASS 28/29).",
    "prohibits": [
        "deprecated",
        "obsolete",
        "non-conforming"
    ],
    "removed": [
        "removed"
    ],
    "scope_exception": [
        "except"
    ],
    "usage_form": [
        "if"
    ],
    "replacement": [
        "replace"
    ],
    "_doc_pass35": "PASS 35 \u2014 the SUPERSESSION register ANCHOR, the fifth datum. A governing sentence that carries the REPLACE meaning (the anchor word, its morphological forms, or a word the dictionary defines via it as a verb) and names ANOTHER living construct in the author's own code typography states 'successor replaces subject'. Owner ruling: supersession is NOT prohibition \u2014 no negation is involved; the relation is read by the meaning net (never a sentence template) and persisted as the node's superseded_by edge. Consumed by lint_web's succession read."
}